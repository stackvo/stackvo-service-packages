[mysqld]
# Character Set & Collation
character-set-server = utf8mb4
collation-server = utf8mb4_unicode_ci

# Performance Settings
max_connections = 200
innodb_buffer_pool_size = 256M
innodb_log_file_size = 64M
innodb_flush_log_at_trx_commit = 2
innodb_flush_method = O_DIRECT

# Query Cache (deprecated in newer versions, but kept for compatibility)
query_cache_type = 1
query_cache_size = 32M
query_cache_limit = 2M

# Logging
# Slow query log disabled for local development
# For debugging, uncomment the following lines to enable stdout logging:
# slow_query_log = 1
# slow_query_log_file = /dev/stdout
long_query_time = 2

# General
sql_mode = "STRICT_TRANS_TABLES,NO_ZERO_IN_DATE,NO_ZERO_DATE,ERROR_FOR_DIVISION_BY_ZERO,NO_ENGINE_SUBSTITUTION"
